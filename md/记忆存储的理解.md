### 1.四类分类 — user / feedback / project / reference，DB 层用 SQLite CHECK 约束（database.ts:57）

## 2.buildMemoryPrompt() 把所有记忆按四类标题拼接 → 超过 25 KB / 200 行 → 回退成只注入 MEMORY.md 索引 没有向量检索、没有关键词过滤、没有按相关性挑选

疑问：“目前没有任何"按当前对话内容召回相关记忆"的能力。如果以后你想升级（按 query 选 5 条），需要在
  createMemoryContext 里加一层 LLM 或简单 TF-IDF 召回。” 这个也不能做到按照四类分类 user / feedback / project / reference的搜索吗？

疑问：“3.2 System Prompt 注入流程

在 `context-store` 里新增一个 `MemoryContextProvider`：

```
会话启动
  ↓
读取 IndexedDB["airi-memory/MEMORY.md"]
  ↓
全量注入（< 200 行 / 25KB）或只注入索引摘要（超限时）
  ↓
附加新鲜度警告（mtime > 1 天的记忆文件）
  ↓
System Prompt = [原有 prompt] + [记忆块]
```

注入格式示例：

```
<memory>
以下是关于用户的持久记忆，请在回复中参考这些信息：

# 用户画像
[user_profile.md 内容]

# 行为纠正记录
[feedback_tone.md 内容]（⚠️ 3 天前，请核实是否仍适用）
</memory>
```

” 实现了吗



记忆系统只负责存储记忆信息，而上下文搜寻策略才是再存储的信息中寻找与这次对话相关的信息。如果是进行四类分类（如按照情感分类）再根据用户当前情感进行搜寻的策略（如用户现在伤心，那么我们搜寻开心的记忆来安慰）如何？



![image-20260428165908124](C:\Users\Midas\AppData\Roaming\Typora\typora-user-images\image-20260428165908124.png)

![image-20260428165919181](C:\Users\Midas\AppData\Roaming\Typora\typora-user-images\image-20260428165919181.png)
